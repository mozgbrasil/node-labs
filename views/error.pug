extends layout

block content
    .container
        .row
            h1= message
            h2= error.status
            <pre class="highlight"><code><samp>
            | #{error.stack}
            </samp></code></pre>
